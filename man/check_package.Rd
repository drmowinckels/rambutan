% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_package.R
\name{check_package}
\alias{check_package}
\title{Check every URL referenced in an R package's sources}
\usage{
check_package(path = ".", options = lychee_options())
}
\arguments{
\item{path}{Path to the root of an R package's source tree. Defaults to
the current directory.}

\item{options}{A \code{lychee_options} object created by \code{\link[=lychee_options]{lychee_options()}}.}
}
\value{
A data frame with one row per checked URL: \code{url}, \code{parent} (the
file the URL was found in), \code{is_success}, \code{code}, and \code{details}.
}
\description{
Scans the same files CRAN's automated URL checks (and
\verb{R CMD check --as-cran}) inspect -- \code{DESCRIPTION}, Rd files in \verb{man/},
\code{CITATION}, \code{NEWS.Rd}/\code{NEWS.md}, \code{README.md}, and built HTML vignettes --
plus unbuilt R Markdown vignette sources, then checks every URL found
with \code{\link[=check_url]{check_url()}}.
}
\examples{
\dontrun{
check_package(".")
}
}
