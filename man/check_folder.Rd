% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_folder.R
\name{check_folder}
\alias{check_folder}
\title{Check links found in every file within a folder}
\usage{
check_folder(
  path,
  recursive = TRUE,
  extensions = NULL,
  options = lychee_options()
)
}
\arguments{
\item{path}{Path to a single existing directory to scan for links.}

\item{recursive}{Single logical. Scan subdirectories as well? Defaults
to \code{TRUE}.}

\item{extensions}{Character vector of file extensions (with or without
the leading dot, e.g. \code{c("md", "html")}) to restrict the scan to, or
\code{NULL} to scan every file lychee can extract links from.}

\item{options}{A \code{lychee_options} object created by \code{\link[=lychee_options]{lychee_options()}}.}
}
\value{
A data frame with one row per discovered link: \code{source}, \code{line},
\code{column}, \code{url}, \code{is_success}, \code{code}, \code{details}.
}
\description{
Convenience wrapper around \code{\link[=check_paths]{check_paths()}} for scanning a directory. By
default the whole directory tree is scanned, exactly like
\code{check_paths(path)}. Set \code{recursive = FALSE} to scan only the files
directly inside \code{path}, or \code{extensions} to restrict the scan to files
with the given extensions; both are implemented as glob patterns handed
to \code{\link[=check_paths]{check_paths()}}, so hidden files/directories and anything matched by
a \code{.gitignore} are skipped in every case, same as the 'lychee' CLI.
}
\examples{
\dontrun{
check_folder("vignettes")
check_folder("vignettes", recursive = FALSE)
check_folder(".", extensions = "md")
}
}
