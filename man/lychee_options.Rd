% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lychee_options.R
\name{lychee_options}
\alias{lychee_options}
\title{Configure optional lychee behaviour}
\usage{
lychee_options(
  exclude = character(),
  include = character(),
  timeout = NULL,
  max_redirects = NULL,
  max_retries = NULL,
  retry_wait_time = NULL,
  user_agent = NULL,
  method = NULL,
  accept = character(),
  exclude_all_private = NULL,
  exclude_private = NULL,
  exclude_link_local = NULL,
  exclude_loopback = NULL,
  require_https = NULL,
  include_mail = NULL,
  header = NULL
)
}
\arguments{
\item{exclude}{Character vector of regular expressions. URLs matching
any pattern are treated as excluded rather than checked.}

\item{include}{Character vector of regular expressions. URLs matching
any pattern are always checked, even if they also match \code{exclude}.}

\item{timeout}{Request timeout in seconds, or \code{NULL} to use the default
(20).}

\item{max_redirects}{Maximum number of redirects to follow, or \code{NULL}
to use the default (10).}

\item{max_retries}{Maximum number of retries per request, or \code{NULL} to
use the default (3).}

\item{retry_wait_time}{Initial wait time in seconds between retries, or
\code{NULL} to use the default (1).}

\item{user_agent}{User agent string sent with every request, or \code{NULL}
to use lychee's default.}

\item{method}{A single HTTP method, e.g. \code{"get"} or \code{"head"}, or \code{NULL}
to use the default (\code{"get"}).}

\item{accept}{Character vector of accepted status codes/ranges, e.g.
\code{c("200..=204", "429")}. Defaults to lychee's own accepted range
when empty.}

\item{exclude_all_private}{Single logical. Exclude all private,
link-local, and loopback IP addresses, or \code{NULL}.}

\item{exclude_private}{Single logical. Exclude private IP addresses,
or \code{NULL}.}

\item{exclude_link_local}{Single logical. Exclude link-local IP
addresses, or \code{NULL}.}

\item{exclude_loopback}{Single logical. Exclude loopback IP addresses,
or \code{NULL}.}

\item{require_https}{Single logical. Treat unencrypted HTTP links as
errors when HTTPS is available, or \code{NULL}.}

\item{include_mail}{Single logical. Also check \verb{mailto:} links, or
\code{NULL}.}

\item{header}{Named character vector of custom HTTP headers sent with
every request, or \code{NULL}.}
}
\value{
A list with class \code{lychee_options}.
}
\description{
Builds the options object accepted by \code{\link[=check_url]{check_url()}}, \code{\link[=check_urls]{check_urls()}},
\code{\link[=check_paths]{check_paths()}}, and \code{\link[=check_package]{check_package()}}. Every argument here shares its
name with the matching field in a \code{lychee.toml} file -- the same
configuration file read by the 'lychee' CLI. When a \code{lychee.toml} is
present in the current working directory it is picked up automatically;
values passed here take precedence over the file, the file takes
precedence over 'lychee-lib's own defaults, and \code{exclude}/\code{include}/
\code{header} are combined with the file rather than replacing it.
}
\examples{
lychee_options(timeout = 10, exclude = "^https://example\\\\.com")
}
