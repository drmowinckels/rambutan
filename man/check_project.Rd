% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_project.R
\name{check_project}
\alias{check_project}
\title{Check links found across an entire project directory}
\usage{
check_project(
  path = ".",
  exclude_dirs = c("node_modules", "renv", "packrat", "target", "dist", "build",
    "vendor"),
  options = lychee_options()
)
}
\arguments{
\item{path}{Path to the root of the project to scan. Defaults to the
current directory.}

\item{exclude_dirs}{Character vector of directory names to skip
anywhere in the tree, in addition to hidden directories, which are
always skipped. Defaults to common dependency and build-artifact
directories (\code{node_modules}, \code{renv}, \code{packrat}, \code{target}, \code{dist},
\code{build}, \code{vendor}).}

\item{options}{A \code{lychee_options} object created by \code{\link[=lychee_options]{lychee_options()}}.}
}
\value{
A data frame with one row per discovered link: \code{source}, \code{line},
\code{column}, \code{url}, \code{is_success}, \code{code}, \code{details}.
}
\description{
Convenience wrapper around \code{\link[=check_paths]{check_paths()}} for scanning a whole project
tree, recursively, while skipping directories that are virtually never
meant to be link-checked -- installed dependencies and build artifacts.
Hidden files and directories (e.g. \code{.git}, \code{.Rproj.user}) are always
skipped. Unlike \code{\link[=check_package]{check_package()}}, \code{check_project()} makes no assumption
that \code{path} is an R package.
}
\examples{
\dontrun{
check_project(".")
}
}
