% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_urls}
\alias{check_urls}
\title{Check multiple URLs concurrently}
\usage{
check_urls(urls, options = lychee_options())
}
\arguments{
\item{urls}{Character vector of URLs to check.}

\item{options}{A \code{lychee_options} object created by \code{\link[=lychee_options]{lychee_options()}}.}
}
\value{
A data frame with one row per element of \code{urls} (in the same
order): \code{url}, \code{is_success}, \code{code}, \code{details}.
}
\description{
Vectorised version of \code{\link[=check_url]{check_url()}}. URLs are checked concurrently rather
than one at a time.
}
\examples{
\dontrun{
check_urls(c("https://www.r-project.org", "https://cran.r-project.org"))
}
}
